import { writable } from 'svelte/store';

export const gamePaused = writable(true);

export { isExploring, exploreWarning } from './explore';

/** 当前游戏是否已完成（无空格且无冲突），由 grid.js syncFromGame() 委托领域层更新 */
export const gameWon = writable(false);
